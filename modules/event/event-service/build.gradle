dependencies {
	compile project(":modules:event:event-api")

	compileOnly group: "com.liferay.portal", name: "release.portal.api"
}

buildService {
	apiDir = "../event-api/src/main/java"
}

group = "event"